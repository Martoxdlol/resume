---
import SectionTitle from "./SectionTitle.astro";

export interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<section class="mb-3 [&_p]:text-sm">
  {title && <SectionTitle>{title}</SectionTitle>}
  <slot />
</section>
